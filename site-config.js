window.SITE_CONFIG = {
  // GitHub Pages keeps this blank and shows the daily market desk.
  // Vercel same-origin API can use "same-origin"; custom API can use "https://your-api.example.com".
  // Local development keeps using http://127.0.0.1:8790 automatically when blank.
  stockApiBase: ""
};
